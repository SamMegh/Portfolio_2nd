import React from 'react'

function RightSection({ className }) {
  return (
   <div className={`${className} h-full overflow-hidden`}>
  this is right section
</div>

  )
}

export default RightSection
